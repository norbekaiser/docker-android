stages:
  - base_images
  - sdk_images
  - ndk_images
create_base_images:
    stage: base_images
    image: norbnorb/vcs
    only:
      - master
    script:
      - git branch
      - git checkout -B base
      - git branch
      - git checkout master Dockerfile
      - git add Dockerfile
      - git commit -am "Updated Dockerfile From Master"
      - git push -u origin base 
